import { Metadata } from "next";
import From from "./From";

export const metadata: Metadata = {
  title: "Register",
};

const Register = () => {
  return (
    <>
      <From />
    </>
  );
};

export default Register;
